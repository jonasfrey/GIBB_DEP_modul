# Lokale Entwicklung
PORT=3000
APP_NAME=dep-myapp-c3
APP_VERSION=1.0.0

# Postgres-Verbindung. Auf Render automatisch durch render.yaml gesetzt.
# Lokal optional: setzen, um echte Persistenz zu testen.
# Beispiel: DATABASE_URL=postgres://depmyapp:secret@localhost:5432/depmyapp
# DATABASE_URL=

# SSL gegen Postgres deaktivieren (nur lokal). Render erzwingt SSL.
# PGSSL=disable
