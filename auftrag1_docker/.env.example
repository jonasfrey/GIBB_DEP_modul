# Pflicht: Postgres-Passwort (compose schlaegt sonst fehl)
POSTGRES_PASSWORD=change-me-in-real-deploy

# Optional (Defaults greifen, wenn leer)
POSTGRES_USER=depmyapp
POSTGRES_DB=depmyapp
NGINX_PORT=8080
