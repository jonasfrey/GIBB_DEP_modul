{
  "name": "dep-myapp-c1-web",
  "version": "1.0.0",
  "description": "Web-Service fuer C1 Multi-Service-Compose-Stack",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "lint": "node --check server.js && node --check db.js && node --check logger.js && node --check test/server.test.js",
    "test": "node --test test/"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.13.1"
  }
}
